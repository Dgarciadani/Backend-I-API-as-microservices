package com.clinic.msappointmentservice.domain.model;

public record Dentist(Long id,
                      String name,
                      String lastName,
                      Integer register) {
}
